<a href="<%= user_path(current_user) %>">
  <%= gravatar_for current_user, size: 52 %>
</a>
<h1>
  <%= current_user.name %>
</h1>
<span>
  <%= link_to "Pokaż mój profil", current_user %>
</span>
<span>
  <%= I18n.t :micropost, :count => current_user.microposts.count %>
</span>
