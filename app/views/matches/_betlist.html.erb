<table class="table table-bordered table-condensed table-striped">
	<thead>
		<tr>
			<th>Kto gra</th>
			<th>Wynik</th>
		</tr>
	</thead>
	<tbody></tbody>
	<% @match.bets.each do |bet| %>
	<tr>
		<td><%= gravatar_for bet.user, {size: 16} %>
		<%= bet.user.name %></td>
		<td><%= bet.score if @match.finished? %></td>
	</tr>
	<% end %>
	</tbody>
</table>